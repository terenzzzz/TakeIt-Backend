PORT=3001
CORS_ORIGIN=http://localhost:5173
# Optional: required for the Douyin browser fallback when Playwright cannot find Chromium.
CHROMIUM_EXECUTABLE_PATH=
# MongoDB connection used to persist extract records. Leave empty to disable saving.
MONGODB_URI=mongodb://takeit:password@127.0.0.1:27017/takeit?authSource=takeit
